-- globals.lua

-- Global configuration table using descriptive names for UI types.
GLOBALS = {
  UIT = {
    ROOT    = "Root",     -- The root node of the UI hierarchy.
    ROW     = "Row",      -- A horizontal container (row) for UI elements.
    COLUMN  = "Column",   -- A vertical container (column) for UI elements.
    TEXT    = "Text",     -- A text element.
    OBJECT  = "Object",   -- A generic object element (e.g. sprite).
  },
  FUNCS = {},             -- Placeholder for callback functions.
  COLOR = {},             -- Placeholder for color definitions.
  -- You can add more fields here as needed.
}
